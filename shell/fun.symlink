# hehe
alias ny='telnet miku.acm.uiuc.edu'
alias rr='curl -L http://bit.ly/10hA8iC | bash'
alias sw='telnet towel.blinkenlights.nl'

if [ $(uname -s) == "Darwin" ]; then
    source ~/.osx.shell/fun
elif [ $(uname -s) == "Linux" ]; then
    source ~/.linux.shell/fun
fi
